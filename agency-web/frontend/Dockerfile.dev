# Development Dockerfile
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source (volume will override)
COPY . .

# Expose port
EXPOSE 5173

# Dev command (overridden in compose)
CMD ["pnpm", "dev", "--host"]
